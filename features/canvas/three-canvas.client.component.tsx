"use client";

/**
 * three-canvas.client.component.tsx
 *
 * Three.js/R3F canvas — Client Component boundary.
 * Returns null in Phase 3; full scene code is added in a future phase.
 * Packages (three, @react-three/fiber, etc.) are installed and ready.
 */

export function ThreeCanvas() {
	return null;
}
